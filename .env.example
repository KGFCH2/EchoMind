# Example `.env` — copy to `.env` and fill with real keys. Do NOT commit `.env`.

# -----------------------------
# Gemini / Generative provider
# -----------------------------
# API key for your primary generative provider (Gemini / Google or similar)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: HTTP endpoint to call for Gemini / generative model. If you set this,
# the assistant will POST {"prompt": "..."} to this URL and attempt to extract
# a human-readable text reply. Example (Google or proxy endpoint):
# https://generativelanguage.googleapis.com/v1/models/your-model:generate
GEMINI_API_ENDPOINT=https://your-host.example.com/v1/generate

# Optional: header name to send the API key in (e.g. x-goog-api-key). If empty
# the client will send `Authorization: Bearer <key>`.
GEMINI_API_KEY_HEADER=

# Enable streaming when supported by endpoint (true/1/yes)
GEMINI_API_STREAM=true

# Ask the model to reply with plain text only (optional)
GEMINI_RESPONSE_MODE=plain_text

# Optional short wrapper instruction prepended to every prompt when using
# `GEMINI_RESPONSE_MODE=plain_text` (keep it brief).
GEMINI_PROMPT_WRAPPER=You are a helpful voice assistant. Provide concise plain-text answers.

# -----------------------------
# Groq (fallback) configuration
# -----------------------------
# Groq API key and endpoint used as an automatic fallback when Gemini quota
# is exhausted. Obtain these from your Groq account dashboard / deployments page.
GROQ_API_KEY=your_groq_api_key_here
GROQ_API_ENDPOINT=https://api.groq.com/openai/v1/chat/completions
# Optionally request plain-text responses from Groq
GROQ_RESPONSE_MODE=plain_text
GROQ_MODEL=llama-3.1-8b-instant  # Examples: llama-3.1-8b-instant, llama-3.3-70b-versatile

# -----------------------------
# Other optional keys
# -----------------------------
# Weather API (OpenWeatherMap) — only required if you enable weather features
OPENWEATHER_API_KEY=your_openweather_api_key_here

# Notes:
# - Copy this file to `.env` and populate real keys. Keep `.env` out of VCS.
# - For Google-style API keys you may need to set `GEMINI_API_KEY_HEADER=x-goog-api-key`.
# - Example endpoints above are placeholders — use the exact URL from provider docs.
