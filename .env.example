# Example .env file — copy to `.env` and fill in your real keys. Do NOT commit `.env`.
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: HTTP endpoint to call for Gemini / generative model. If you set this, the assistant will POST {"prompt": "..."} to this URL with Authorization: Bearer $GEMINI_API_KEY and attempt to extract a text reply.
# Example (Google or proxy endpoint): https://your-host.example.com/v1/generate
GEMINI_API_ENDPOINT=https://your-host.example.com/v1/generate
# Optional 
GEMINI_API_KEY_HEADER=

# Set (true)
GEMINI_API_STREAM=true

# Response formatting options (optional)
# Set GEMINI_RESPONSE_MODE=plain_text to instruct the model to reply with only the final answer as plain text (no JSON, no code fences, no extra metadata).
GEMINI_RESPONSE_MODE=plain_text

# Optional: custom wrapper instruction prepended to every prompt when GEMINI_RESPONSE_MODE is set. Keep it short and explicit.
GEMINI_PROMPT_WRAPPER=You are a helpful voice assistant. Provide complete, detailed answers. Do not include JSON, code blocks, or formatting - just plain text.

# Weather API key (optional, only needed if you want weather info)
OPENWEATHER_API_KEY=your_openweather_api_key_here
